import { eq } from "drizzle-orm";
import { db } from "@/db";
import { availabilityRules,practitioners,practitionerTimeOff } from "@/db/schema";
import { getCurrentAdmin,hasAdminPermission,recordAudit } from "@/lib/admin-auth";
import { getPractitionerDirectory } from "@/lib/scheduling";

export async function PUT(request:Request,{params}:{params:Promise<{id:string}>}){const admin=await getCurrentAdmin();if(!admin)return Response.json({error:"Administrator access required."},{status:401});if(!hasAdminPermission(admin,"schedule"))return Response.json({error:"Scheduling permission required."},{status:403});const{id:raw}=await params;const id=Number(raw);if(!Number.isInteger(id)||id<1)return Response.json({error:"Invalid practitioner id."},{status:400});const[person]=await db.select().from(practitioners).where(eq(practitioners.id,id)).limit(1);if(!person)return Response.json({error:"Practitioner not found."},{status:404});const body=await request.json() as {rules?:Array<{weekday:number;startTime:string;endTime:string;active?:boolean}>;timeOff?:Array<{startsAt:string;endsAt:string;reason?:string}>};const rules=(body.rules??[]).filter(rule=>Number.isInteger(rule.weekday)&&rule.weekday>=0&&rule.weekday<=6&&/^\d{2}:\d{2}$/.test(rule.startTime)&&/^\d{2}:\d{2}$/.test(rule.endTime)&&rule.startTime<rule.endTime);const timeOff=(body.timeOff??[]).map(item=>({startsAt:new Date(item.startsAt),endsAt:new Date(item.endsAt),reason:item.reason?.trim().slice(0,180)||null})).filter(item=>!Number.isNaN(item.startsAt.getTime())&&!Number.isNaN(item.endsAt.getTime())&&item.startsAt<item.endsAt);await db.transaction(async tx=>{await tx.delete(availabilityRules).where(eq(availabilityRules.practitionerId,id));await tx.delete(practitionerTimeOff).where(eq(practitionerTimeOff.practitionerId,id));if(rules.length)await tx.insert(availabilityRules).values(rules.map(rule=>({...rule,practitionerId:id,active:rule.active??true})));if(timeOff.length)await tx.insert(practitionerTimeOff).values(timeOff.map(item=>({...item,practitionerId:id})));});await recordAudit(admin,"practitioner.schedule_updated","practitioner",id,{weeklyRules:rules.length,timeOffBlocks:timeOff.length});const all=await getPractitionerDirectory(false);return Response.json(all.find(item=>item.id===id));}
