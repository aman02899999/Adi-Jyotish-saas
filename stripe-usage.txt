src/db/schema.ts:224:export const stripeEvents = pgTable("stripe_events", {
src/db/schema.ts:226:  stripeEventId: varchar("stripe_event_id", { length: 180 }).notNull().unique(),
src/proxy.ts:6:  if (!unsafeMethods.has(request.method) || request.nextUrl.pathname === "/api/webhooks/stripe") {
src/app/admin/(protected)/billing/page.tsx:5:import { isStripeConfigured } from "@/lib/stripe";
src/app/admin/(protected)/billing/page.tsx:12:  return <AdminShell active="Billing"><div className="admin-content"><div className="admin-heading"><div><p>Jyotish / Finance</p><h1>Billing</h1><span>Collect, reconcile, and refund consultation revenue.</span></div><div><small>Ledger status</small><strong>Live <i/></strong></div></div><AdminBilling initialInvoices={rows} stripeConfigured={isStripeConfigured()}/></div></AdminShell>;
src/app/dashboard/billing/page.tsx:5:import { isStripeConfigured } from "@/lib/stripe";
src/app/api/member/invoices/route.ts:3:import { isStripeConfigured } from "@/lib/stripe";
src/app/api/member/invoices/[id]/checkout/route.ts:5:import { getStripe } from "@/lib/stripe";
src/app/api/member/invoices/[id]/checkout/route.ts:22:  const stripe = getStripe();
src/app/api/member/invoices/[id]/checkout/route.ts:23:  if (!stripe) return Response.json({ error: "Online payments are not configured. You can still pay through the studio." }, { status: 503 });
src/app/api/member/invoices/[id]/checkout/route.ts:26:  const session = await stripe.checkout.sessions.create({
src/app/api/member/invoices/[id]/checkout/route.ts:37:  await db.insert(payments).values({ invoiceId: invoice.id, bookingId: booking.id, amount: invoice.amount, currency: invoice.currency, provider: "stripe", status: "pending", providerSessionId: session.id });
src/app/api/invoices/route.ts:3:import { isStripeConfigured } from "@/lib/stripe";
src/app/api/invoices/route.ts:11:  return Response.json({ invoices: await getAdminBilling(), stripeConfigured: isStripeConfigured() });
src/app/api/invoices/[id]/route.ts:6:import { getStripe } from "@/lib/stripe";
src/app/api/invoices/[id]/route.ts:104:    if (claimed.payment.provider === "stripe") {
src/app/api/invoices/[id]/route.ts:105:      const stripe = getStripe();
src/app/api/invoices/[id]/route.ts:106:      if (!stripe || !claimed.payment.paymentIntentId) throw new Error("Stripe refund is unavailable for this payment.");
src/app/api/invoices/[id]/route.ts:107:      const refund = await stripe.refunds.create({ payment_intent: claimed.payment.paymentIntentId, reason: "requested_by_customer", metadata: { invoiceId: String(claimed.invoice.id), bookingId: String(claimed.booking.id) } }, { idempotencyKey: `invoice-refund-${claimed.invoice.id}` });
src/components/admin-billing.tsx:9:export function AdminBilling({initialInvoices,stripeConfigured}:{initialInvoices:FinanceInvoice[];stripeConfigured:boolean}){const[items,setItems]=useState(initialInvoices);const[query,setQuery]=useState("");const[filter,setFilter]=useState("all");const[notice,setNotice]=useState("");const[saving,setSaving]=useState<number|null>(null);const filtered=useMemo(()=>items.filter(x=>`${x.number} ${x.customerName} ${x.customerEmail} ${x.description}`.toLowerCase().includes(query.toLowerCase())&&(filter==="all"||x.status===filter)),[items,query,filter]);const stats=useMemo(()=>({collected:items.filter(x=>x.status==="paid").reduce((s,x)=>s+x.amount,0),outstanding:items.filter(x=>x.status==="open").reduce((s,x)=>s+x.amount,0),refunded:items.filter(x=>x.status==="refunded").reduce((s,x)=>s+x.amount,0),paid:items.filter(x=>x.status==="paid").length}),[items]);
src/components/admin-billing.tsx:11:  return <><section className="finance-stats"><article><span><CircleDollarSign size={20}/></span><div><small>Collected</small><strong>${stats.collected.toLocaleString()}</strong><p>{stats.paid} paid invoices</p></div></article><article><span><CreditCard size={20}/></span><div><small>Outstanding</small><strong>${stats.outstanding.toLocaleString()}</strong><p>Awaiting collection</p></div></article><article><span><Undo2 size={20}/></span><div><small>Refunded</small><strong>${stats.refunded.toLocaleString()}</strong><p>Lifetime adjustments</p></div></article><article><span><ShieldCheck size={20}/></span><div><small>Online payments</small><strong>{stripeConfigured?"Live":"Offline"}</strong><p>{stripeConfigured?"Stripe connected":"Manual collection enabled"}</p></div></article></section>{!stripeConfigured&&<div className="finance-config-note"><ShieldCheck size={18}/><div><strong>Manual mode is active</strong><span>Add server-side STRIPE_SECRET_KEY and STRIPE_WEBHOOK_SECRET values to enable hosted Checkout and automatic reconciliation.</span></div></div>}<section className="admin-table-card"><div className="admin-table-header"><div><h2>Invoice ledger</h2><p>Collect, reconcile, void, and refund consultation invoices.</p></div><span className="finance-live"><i/>Live ledger</span></div><div className="admin-toolbar"><label><Search size={16}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Invoice, customer, email or service…"/></label><div className="filter-select"><select value={filter} onChange={e=>setFilter(e.target.value)}><option value="all">All statuses</option><option value="open">Open</option><option value="paid">Paid</option><option value="refunded">Refunded</option><option value="refund_pending">Refund pending</option><option value="void">Void</option></select><ChevronDown size={14}/></div><span>{filtered.length} invoices</span></div><div className="finance-table"><div className="finance-table__head"><span>Invoice</span><span>Customer</span><span>Amount</span><span>Due / paid</span><span>Status</span><span>Method</span><span>Actions</span></div>{filtered.map(invoice=>{const payment=invoice.payments[0];return <div className="finance-table__row" key={invoice.id}><div className="finance-invoice"><span><FileCheck2 size={16}/></span><div><strong>{invoice.number}</strong><small>{invoice.description}</small><i>{invoice.bookingReference}</i></div></div><div className="finance-customer"><strong>{invoice.customerName}</strong><small>{invoice.customerEmail}</small></div><strong>{invoice.currency} {invoice.amount}</strong><div className="finance-date"><strong>{new Date(invoice.paidAt||invoice.dueAt).toLocaleDateString("en",{month:"short",day:"numeric",year:"numeric"})}</strong><small>{invoice.paidAt?"Paid":"Due"}</small></div><span className={`invoice-status invoice-status--${invoice.status}`}>{invoice.status.replace("_"," ")}</span><span className="payment-provider">{payment?.provider||"—"}</span><div className="finance-actions">{invoice.status==="open"&&<><button disabled={saving===invoice.id} onClick={()=>action(invoice,"mark_paid")}>Collect</button><button onClick={()=>action(invoice,"void")}>Void</button></>}{invoice.status==="paid"&&<button className="refund" disabled={saving===invoice.id} onClick={()=>action(invoice,"refund")}><RefreshCcw size={12}/>Refund</button>}{invoice.status==="void"&&<button onClick={()=>action(invoice,"reopen")}>Reopen</button>}</div></div>})}{!filtered.length&&<div className="empty-state"><FileCheck2 size={25}/><h3>No invoices found</h3><p>Try a different filter or search.</p></div>}</div></section>{notice&&<div className="toast"><Check size={15}/>{notice}<button onClick={()=>setNotice("")}><X size={14}/></button></div>}</>;
